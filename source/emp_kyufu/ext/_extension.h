
#ifndef EXTENSION_H_INCLUDED
#define EXTENSION_H_INCLUDED

#define FOR(name, from, to) for(int name = from; name <= to; name++)

#define GLOBAL_FOR(name, from, to) for(name = from; name <= to; name++)

#define REV_FOR(name, upper, lower) for(int name = upper; name >= lower; name--)

#endif